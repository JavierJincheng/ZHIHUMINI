package com.zhihu.util;



/**
 * Created by 木木高 on 2017/7/25.
 */
public class IfLoginUtil {
   // LoginSessionLisenler LisenerTest=new LoginSessionLisenler();
    public void isLogonUser(String userId) {
    /* Set<HttpSession> keys = LisenerTest.loginUser.keySet();

        for (HttpSession key : keys) {
            System.out.println("-----------"+key);
            if (*//*LisenerTest.loginUser.get(key)*//*"root".trim().equals(userId)) {
                return true;
            }
        }
        return false;*/

    }
}
